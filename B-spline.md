三つとも数学で使われる用語ですが、役割が異なります。**Bスプラインは面の表現方法、L2射影は元の場に最も近い場を求める操作**です。理解の順序として、Bスプラインから説明します。

1. **Bスプライン**

   スプラインは、区間ごとの多項式をつないで曲線を表す方法です。Bスプラインは、その曲線を組み立てるための基底関数です。

   基底関数を \(B_j(x)\)、係数を \(c_j\) とすると、曲線を次の形で表します。

   \[
   f(x)=\sum_j c_j B_j(x)
   \]

   各基底関数は限られた区間で値を持ち、係数によって曲線への寄与を調整します。区間の境界を「節点」、多項式の次数を「次数」と呼びます。[SciPyの解説](https://docs.scipy.org/doc/scipy/tutorial/interpolate/splines_and_polynomials.html)

   今回は、磁束から共エネルギー面の係数を求めるために使っています。すべての観測点を必ず通るわけではないため、報告書では「Bスプライン補間」よりも、**「Bスプラインによる近似」**が適切です。

2. **テンソル積3次Bスプライン**

   「3次」は、各区間を3次以下の多項式で表すことを意味します。「テンソル積」は、各方向の基底関数を掛け合わせて、多変数の関数へ拡張することです。[SciPyの多変数スプラインの定義](https://docs.scipy.org/doc/scipy/reference/generated/scipy.interpolate.NdBSpline.html)

   今回の二つの変数は、d軸電流とq軸電流です。基本的な構造は次のようになります。

   \[
   W(i_d,i_q)=\sum_j\sum_k c_{jk}B_j(i_d)B_k(i_q)
   \]

   d軸方向の基底関数とq軸方向の基底関数を掛け、その組合せを足し合わせて面を作ります。これは構造を説明する式であり、実際のモデルには低次項や対称性の処理も含まれます。

   「3次」は**各変数についての次数**です。「3次元」を意味するものでも、二変数を合わせた総次数が3という意味でもありません。

   現在の命名は妥当ですが、**「テンソル積型の3次Bスプライン」**とすると語の関係が読み取りやすくなります。初出には、次の説明を添えると伝わります。

   > d軸方向とq軸方向の3次Bスプライン基底を掛け合わせ、共エネルギー面を表す。

3. **L2射影**

   「射影」は、ある条件を満たす集合の中から、元の対象に最も近いものを求める操作です。L2射影では、差の二乗を領域全体で積分した量によって近さを測ります。[ペンシルベニア大学の射影の解説](https://www2.math.upenn.edu/~gressman/analysis/10-projections.html)

   今回は、「単一面の勾配で表される磁束場」という条件を満たす保存場の中から、元の磁束場に最も近いものを求めています。

   \[
   \int_\Omega
   \left\|\boldsymbol{\psi}-\nabla W\right\|^2\,dA
   \quad\text{を最小にする}
   \]

   その保存場を元の磁束場から引いた残りが、報告書でいう「非保存成分」です。

   **「L2射影」だけでは、何へ射影するのかが分かりません。** 今回の初出は、**「保存場へのL2射影」**とするのが適切です。

   > 元の磁束場との差の二乗積分が最小となる保存場を求める。この操作を、保存場へのL2射影と呼ぶ。

報告書では、次の呼び分けを勧めます。

| 用語 | 報告書での呼び方 |
|---|---|
| Bスプライン | Bスプライン基底／Bスプラインによる近似 |
| テンソル積3次Bスプライン | テンソル積型の3次Bスプライン |
| L2射影 | 初出は「保存場へのL2射影」、以後は「L2射影」 |

なお、**相反性を保証するのは、単一の滑らかな面を微分して磁束を求める構造**です。Bスプラインはその面を表現する方法であり、L2射影は不整合を評価するための方法です。
